$kind: http-request
name: "[GET] api/memberships"
url: "{{base_url}}/memberships"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
