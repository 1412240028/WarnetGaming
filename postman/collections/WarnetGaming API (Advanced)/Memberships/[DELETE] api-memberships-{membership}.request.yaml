$kind: http-request
name: "[DELETE] api/memberships/{membership}"
url: "{{base_url}}/memberships/:membership"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  membership: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
