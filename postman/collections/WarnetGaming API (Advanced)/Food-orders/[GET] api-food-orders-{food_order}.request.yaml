$kind: http-request
name: "[GET] api/food-orders/{food_order}"
url: "{{base_url}}/food-orders/:food_order"
method: GET
headers:
  Accept: application/json
pathVariables:
  food_order: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
