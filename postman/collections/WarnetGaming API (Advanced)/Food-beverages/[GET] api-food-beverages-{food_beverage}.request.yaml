$kind: http-request
name: "[GET] api/food-beverages/{food_beverage}"
url: "{{base_url}}/food-beverages/:food_beverage"
method: GET
headers:
  Accept: application/json
pathVariables:
  food_beverage: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
