$kind: http-request
name: "[DELETE] api/payments/{payment}"
url: "{{base_url}}/payments/:payment"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  payment: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
