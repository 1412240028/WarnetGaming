$kind: http-request
name: "[PUT] api/payments/{payment}"
url: "{{base_url}}/payments/:payment"
method: PUT
headers:
  Accept: application/json
pathVariables:
  payment: "1"
body:
  type: json
  content: |-
    {
        
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
