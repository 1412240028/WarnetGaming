$kind: http-request
name: "[GET] api/payments/{payment}"
url: "{{base_url}}/payments/:payment"
method: GET
headers:
  Accept: application/json
pathVariables:
  payment: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
