$kind: http-request
name: "[PUT] api/rooms/{room}"
url: "{{base_url}}/rooms/:room"
method: PUT
headers:
  Accept: application/json
pathVariables:
  room: "1"
body:
  type: json
  content: |-
    {
        
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
