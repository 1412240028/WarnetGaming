$kind: http-request
name: "[GET] api/rooms/{room}"
url: "{{base_url}}/rooms/:room"
method: GET
headers:
  Accept: application/json
pathVariables:
  room: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
