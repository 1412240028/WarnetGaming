$kind: http-request
url: "{{base_url}}/gaming-sessions/{{session_id}}/games"
method: GET
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 1000
