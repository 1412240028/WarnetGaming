$kind: http-request
name: "[GET] api/operators/{operator}"
url: "{{base_url}}/operators/:operator"
method: GET
headers:
  Accept: application/json
pathVariables:
  operator: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
