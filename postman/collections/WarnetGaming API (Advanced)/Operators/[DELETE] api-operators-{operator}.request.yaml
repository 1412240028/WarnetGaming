$kind: http-request
name: "[DELETE] api/operators/{operator}"
url: "{{base_url}}/operators/:operator"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  operator: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
