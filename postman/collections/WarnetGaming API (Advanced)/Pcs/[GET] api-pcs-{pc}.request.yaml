$kind: http-request
name: "[GET] api/pcs/{pc}"
url: "{{base_url}}/pcs/:pc"
method: GET
headers:
  Accept: application/json
pathVariables:
  pc: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
