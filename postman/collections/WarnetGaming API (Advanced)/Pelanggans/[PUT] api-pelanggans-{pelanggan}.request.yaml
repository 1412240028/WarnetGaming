$kind: http-request
name: "[PUT] api/pelanggans/{pelanggan}"
url: "{{base_url}}/pelanggans/:pelanggan"
method: PUT
headers:
  Accept: application/json
pathVariables:
  pelanggan: "1"
body:
  type: json
  content: |-
    {
        
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
