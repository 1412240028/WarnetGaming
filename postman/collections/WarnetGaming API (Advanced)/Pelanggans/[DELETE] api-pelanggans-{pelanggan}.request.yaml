$kind: http-request
name: "[DELETE] api/pelanggans/{pelanggan}"
url: "{{base_url}}/pelanggans/:pelanggan"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  pelanggan: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
