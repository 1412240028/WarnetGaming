$kind: http-request
name: "[GET] api/games/{game}"
url: "{{base_url}}/games/:game"
method: GET
headers:
  Accept: application/json
pathVariables:
  game: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
