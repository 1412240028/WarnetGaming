$kind: http-request
name: "[POST] api/food-beverages"
url: "{{base_url}}/food-beverages"
method: POST
headers:
  Accept: application/json
body:
  type: json
  content: |-
    {
        
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
