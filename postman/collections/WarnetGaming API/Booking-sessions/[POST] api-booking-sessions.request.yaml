$kind: http-request
name: "[POST] api/booking-sessions"
url: "{{base_url}}/booking-sessions"
method: POST
headers:
  Accept: application/json
body:
  type: json
  content: |-
    {
        
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
