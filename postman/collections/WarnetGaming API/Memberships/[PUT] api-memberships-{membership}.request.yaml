$kind: http-request
name: "[PUT] api/memberships/{membership}"
url: "{{base_url}}/memberships/{membership}"
method: PUT
headers:
  Accept: application/json
body:
  type: json
  content: |-
    {
        
    }
order: 4000
