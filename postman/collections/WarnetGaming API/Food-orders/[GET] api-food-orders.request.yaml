$kind: http-request
name: "[GET] api/food-orders"
url: "{{base_url}}/food-orders"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
