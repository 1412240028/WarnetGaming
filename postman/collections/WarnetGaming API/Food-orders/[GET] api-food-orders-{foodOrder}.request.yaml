$kind: http-request
name: "[GET] api/food-orders/{foodOrder}"
url: "{{base_url}}/food-orders/{foodOrder}"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
