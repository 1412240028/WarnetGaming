$kind: http-request
name: "[POST] api/gaming-sessions/{gamingSessionId}/games"
url: "{{base_url}}/gaming-sessions/{gamingSessionId}/games"
method: POST
headers:
  Accept: application/json
body:
  type: json
  content: |-
    {
        
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
