$kind: http-request
name: "[PUT] api/payments/{payment}"
url: "{{base_url}}/payments/{payment}"
method: PUT
headers:
  Accept: application/json
body:
  type: json
  content: |-
    {
        
    }
order: 4000
